#include <bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin>>t;    //testcases
    while(t--)
    {
        string s;
        cin>>s;
        cout<<((s[0]-'0')+(s[s.length()-1]-'0'))<<'\n';   //using string to avoid loops
    }
	return 0;
}
